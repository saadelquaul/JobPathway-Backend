spring.application.name=JobPathway

# Active Profile (default: dev for local development)
spring.profiles.active=dev

# JWT
jwt.secret=JobPathwaySecretKey2026VeryLongSecretKeyForHS256AlgorithmMinimum256Bits
jwt.expiration=86400000

# File Upload
file.upload-dir=uploads/resumes
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
